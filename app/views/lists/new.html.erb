<div class="container">
  <h1>Add a New List</h1>
  <%= simple_form_for @list do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit %>
  <% end %>
  <%= link_to "Back to Home", lists_path %>
</div>
